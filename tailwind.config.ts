import type { Config } from "tailwindcss";

const config: Config = {
  content: [],
  darkMode: "class",
  theme: {
    extend: {
      colors: {},
    },
  },
  plugins: [],
};
export default config;
